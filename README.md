# enomy-wdf

GitHub Pages entrypoint now lives at the repository root (`/index.html`) and redirects to the main site:

- `enomy-finance-site/index.html`

If your Pages source is set to `main` / root, this ensures `https://exor10.github.io/enomy-wdf/` loads correctly.
